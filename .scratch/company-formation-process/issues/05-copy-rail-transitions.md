> *This was generated by AI during triage.*

# Issue: Left Copy Rail Overlay & Step Transition Choreography

Status: resolved
Type: task

## Parent
[PRD.md](file:///d:/Projects/Website-jindal2/.scratch/company-formation-process/PRD.md)

## What to build
Build the left-aligned copy rail and implement the transition choreography for the 4 company formation process steps. Only one step must be visible at a time. The transition between steps must be staggered: as step N exits, it fades out and translates upwards; step N+1 enters after a 4% scroll progress "breathing gap" by translating up from below and fading in. The step index counter (e.g. `01 / 04`) must persist in a static position and update its text content at the exact midpoint of this gap. A progress indicator rail on the right fills in proportion to overall section scroll progress. Implement all transitions imperatively in the ScrollTrigger update callback to avoid React re-renders.

## Acceptance criteria
- [x] Only one step text is visible at a time, positioned inside the copy rail on the left
- [x] Transitions between steps show a staggered fade/translate with a 4% scroll breathing gap
- [x] HUD counter (e.g., `01 / 04`) updates at the exact midpoint of the transition gap
- [x] CSS gradient scrim overlay keeps text legible across all busy visual phases (especially during glazing)
- [x] Right-side scroll progress indicator fills smoothly and matches overall scroll progress
- [x] Visual transitions do not trigger React re-renders and operate smoothly at 60fps

## Blocked by
- [04-scrolltrigger-pin-scrub.md](file:///d:/Projects/Website-jindal2/.scratch/company-formation-process/issues/04-scrolltrigger-pin-scrub.md)

## Answer
We built the text rail, progress bars, and transition animations:
- Removed CSS transition styles for `opacity` and `transform` on the `.process-step` element to allow instantaneous updates in response to ScrollTrigger.
- Implemented transition calculations inside `updateHUD()` using direct style manipulation:
  - Fading in: starts at `rel = 0.04`, fully active at `rel = 0.14`.
  - Hold: fully visible from `rel = 0.14` to `0.92`.
  - Fading out: exits between `rel = 0.92` and `1.00`.
  - Future/Passed steps are completely hidden (`opacity: 0`).
  - This leaves a 4% transition gap where step N is faded out and step N+1 is not yet faded in, letting the background construct details shine.
- Calculated the exact midpoints of these 4% gaps to update the HUD counter (`01 / 04`, `02 / 04`, etc.) text at `progress` levels `0.245`, `0.496`, and `0.792`.
- Updated the right scroll progress bar heights imperatively using `progress * 100`.
- Verified clean compilation with no React render cycle hooks.

