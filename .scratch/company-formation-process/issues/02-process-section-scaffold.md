> *This was generated by AI during triage.*

# Issue: ProcessSection Base Scaffold, Responsive Layout & Accessibility Fallback

Status: resolved
Type: task

## Parent
[PRD.md](file:///d:/Projects/Website-jindal2/.scratch/company-formation-process/PRD.md)

## What to build
Create the base UI scaffolding for the `ProcessSection` component and integrate it directly below the existing `Services` component. Implement two core fallbacks:
1. **Accessibility Fallback:** Check for `prefers-reduced-motion` and, if active, disable the canvas rendering/scrolling code entirely. Render the final completed-building frame as a static background image with the 4 steps readable in standard layout.
2. **Mobile Layout Fallback:** Below 768px viewports, reduce the scroll heights and use a simpler layout structure (such as auto-advancing frames via a timer when in view or sampling every 2nd frame) to avoid heavy mobile scroll paint bottlenecks.

## Acceptance criteria
- [x] `ProcessSection` is mounted immediately below the `Services` component in the main page flow
- [x] If `prefers-reduced-motion` is active, no canvas animation loops run and the static fallback is cleanly styled and readable
- [x] On viewports < 768px, mobile optimization is active (reduced height, optimized frame sampling or disabled pin)
- [x] Component compiles and passes `npm run build` cleanly

## Blocked by
- [01-asset-extraction-pipeline.md](file:///d:/Projects/Website-jindal2/.scratch/company-formation-process/issues/01-asset-extraction-pipeline.md)

## Answer
We created `src/components/ProcessSection.jsx` and `src/components/ProcessSection.css`.
- Mounted `<ProcessSection />` immediately below `<Services />` inside `src/App.jsx`.
- Added media queries for mobile viewports below 768px to scale down height to 240vh and resize copy texts.
- Implemented `prefers-reduced-motion` detection logic that bypasses the canvas scrubbing completely and renders a styled static grid fallback containing all 4 steps.
- Tested compilation and build using `npm run build`, which compiled successfully.

