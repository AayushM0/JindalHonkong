Status: resolved
Type: task
Blocked by: 01

# Issue 02: Unified ScrollTrigger Scrub Timeline

> *This was generated by AI during triage.*

## Parent

- [.scratch/cinematic-hero/PRD.md](file:///d:/Projects/Website-jindal2/.scratch/cinematic-hero/PRD.md)

## What to build

Setup the module-scope ScrollTrigger registration and create a single unified timeline `gsap.timeline()` inside the component's layout effect:
1. **Trigger Configuration**:
   - Trigger element: `.hero-video-scrub-container`.
   - Settings: `pin: true`, `pinSpacing: true`, `scrub: 1`, `start: "top top"`, `end: "+=150%"`, `invalidateOnRefresh: true`, `anticipatePin: 1`.
2. **Timeline scrubbing**:
   - Drive a plain numeric object: `const obj = { frame: 0 }` to `obj.frame = 270`.
   - On timeline tick (`onUpdate`), round the frame index to the nearest integer and execute the canvas redraw function.
   - Prevent redundant canvas draw operations if the rounded frame index hasn't advanced or changed.
   - If target frame isn't loaded yet, default to drawing the nearest lower loaded index to avoid stutter.

## Acceptance criteria

- [ ] Viewport pins smoothly on scroll.
- [ ] Timeline scrub drives canvas redraws based on rounded frame indices.
- [ ] No redundant draws are triggered.
- [ ] No blank canvas frames are painted if scrolling is faster than preloading.
