Status: resolved
Type: task
Blocked by: None - can start immediately

# Issue 01: Canvas Preloading & Aspect-Cover Drawing Setup

> *This was generated by AI during triage.*

## Parent

- [.scratch/cinematic-hero/PRD.md](file:///d:/Projects/Website-jindal2/.scratch/cinematic-hero/PRD.md)

## What to build

Scaffold the Canvas layout inside `HeroVideoScrub.jsx`. Store references to preloaded image files in an array index.
1. **Preload Strategy**:
   - Total frames: `271`.
   - Blocking stage: Preload first 15 frames (`ezgif-frame-001.jpg` to `ezgif-frame-015.jpg`). Once loaded, trigger state change `isReady = true` to render the canvas.
   - Background stage: Preload remaining frames (`016` to `271`) dynamically.
2. **Canvas Aspect-Cover Drawing**:
   - Scale actual canvas attributes by `window.devicePixelRatio`.
   - Write crop calculation mimicking CSS `object-fit: cover` to prevent image stretching/warping.
   - Guard against empty loads: draw the nearest lower loaded index if target frame isn't ready.
   - Initialize overlay structures and styles using `gsap.set()`.

## Acceptance criteria

- [ ] `HeroVideoScrub.jsx` is created.
- [ ] First 15 frames are preloaded before displaying the canvas, preventing white flashes.
- [ ] Remainder of frames load dynamically in the background.
- [ ] Crop algorithm draws frames crisp and undistorted on high-DPI screens.
- [ ] Text contrast meets WCAG AA (≥ 4.5:1).
