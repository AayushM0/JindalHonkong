Status: resolved
Type: task
Blocked by: 03

# Issue 04: Mobile Breakpoints & prefers-reduced-motion Overrides

> *This was generated by AI during triage.*

## Parent

- [.scratch/cinematic-hero/PRD.md](file:///d:/Projects/Website-jindal2/.scratch/cinematic-hero/PRD.md)

## What to build

Implement responsive variants and accessibility overrides using `ScrollTrigger.matchMedia()` inside the layout effect:
1. **Mobile Optimization (`max-width: 767px`)**:
   - Shorten pin range (`end: "+=90%"`).
   - Render optimized frame loading or skipping sequences if scrolling performance becomes bottlenecked.
2. **Reduced Motion Override (`prefers-reduced-motion: reduce`)**:
   - Bypasses triggers, timelines, preloads, and canvas redraw loops entirely.
   - Immediately renders a single static snapshot (e.g. frame `135`) in the center canvas container, with overlays shown at their final states.
3. **Integration**:
   - Import and integrate `HeroVideoScrub.jsx` inside `App.jsx`, removing the previous `HeroSection` component.

## Acceptance criteria

- [ ] System reduced motion setting displays static midpoint frame instantly with no scroll locking.
- [ ] Mobile screen sizes run a shorter pinned scroll range.
- [ ] Code compiles cleanly and integrates into the main app.
