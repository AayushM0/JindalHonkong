Status: resolved
Type: task
Blocked by: 01

# Issue 04: Core Modules Grid

> *This was generated by AI during triage.*

## Parent

- [.scratch/theme-redesign/PRD.md](file:///d:/Projects/Website-jindal2/.scratch/theme-redesign/PRD.md)

## What to build

Rebuild the services display in `Services.jsx` using the strict 3-column enterprise grid.
1. **Grid Setup**: Update the card structure to render in a strict 3-column layout at desktop widths (`md:grid-cols-3`).
2. **Card Styling**:
   - Limit card corner radius to exactly `12px`.
   - Apply a standard `1px solid #e2e8f0` border and remove any heavy gradients.
   - Embed service icons in a tinted square box (e.g. Blue background for core setup, Emerald for taxation & auditing compliance).
3. **Hover States**:
   - Configure a clean hover transition using `cubic-bezier(0.4, 0, 0.2, 1)`.
   - On hover, cards transition to a white background with a soft shadow (`shadow-xl shadow-slate-200/50`) and a slight scale/translate.
4. **Copy integration**: Render the 6-8 core services described in `businessDetails.md` (Company Formation, Accounting, Corporate Secretarial, Banking, Taxation, Auditing, Listing Prep, Tech Infrastructure).

## Acceptance criteria

- [ ] Services section is structured into a strict 3-column layout.
- [ ] Each card has exactly 12px border radius.
- [ ] Icons display inside tinted square backdrops.
- [ ] Card hover states transition smoothly using the custom cubic-bezier.
- [ ] No gradient fills are present on card surfaces.
