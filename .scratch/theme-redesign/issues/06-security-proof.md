Status: resolved
Type: task
Blocked by: 01

# Issue 06: Security Proof Section

> *This was generated by AI during triage.*

## Parent

- [.scratch/theme-redesign/PRD.md](file:///d:/Projects/Website-jindal2/.scratch/theme-redesign/PRD.md)

## What to build

Create `SecuritySection.jsx` containing the trust indicators and the interactive policy toggle simulator.
1. **Layout Structure**: Implement a split 2-column layout.
2. **Left Column (Certifications & Compliance)**:
   - Render a list of regulatory checks (e.g. TCSP Licensed Registry Secretary, HK FRS Accountant Assured, GDPR Compliant Portal, Nominee Trust Encrypted) with blue check-circle icons.
3. **Right Column (Interactive Policy Toggles)**:
   - Render a detail card containing three settings: `Multi-Factor Authentication (MFA)`, `IP Address Whitelisting`, and `Client Key Rotation`.
   - Implement the **Glass-Morphism Toggle** style for the switches:
     - Pill track size: `40px` width.
     - Track color turns to Primary Blue (`#2563eb`) when switched "On".
     - Thumb is a white circle with a subtle shadow, offset by `4px` from the edges (`left-1` when Off, `left-5` or `right-1` when On).
     - Implement a hover glow effect that slightly brightens the active track.
4. **Threat Alert Card**:
   - Render an overlapping small alert card styled with a Slate 800 background, displaying: "Threat Blocked: Unauthorized IP access denied."
   - Position this card absolute, offset over the toggle panel to create a multi-layered depth.

## Acceptance criteria

- [ ] Security section uses a responsive 2-column layout.
- [ ] Blue check-circle icons are aligned correctly with compliance checklist points.
- [ ] Toggles are interactive and switch states cleanly with animation.
- [ ] Toggle track glows slightly on hover.
- [ ] "Threat Blocked" alert card uses Slate 800 and is layered offset above the main container.
