Status: resolved
Type: task
Blocked by: 01

# Issue 03: Hero & Dashboard Preview Component

> *This was generated by AI during triage.*

## Parent

- [.scratch/theme-redesign/PRD.md](file:///d:/Projects/Website-jindal2/.scratch/theme-redesign/PRD.md)

## What to build

Implement the main landing section in `Hero.jsx` which contains the hero copywriting and the interactive simulated portal dashboard preview.
1. **Hero Setup**:
   - Apply `128px` top padding (`pt-32`) and a centered text structure.
   - Inject a faint `40px` gray grid line pattern overlay on the background.
   - Render a pill-shaped badge at the top (`#eff6ff` background, primary blue text) with a pulsing green status dot: "System Online: Incorporate in 1-3 Days".
   - Render the title: "Your Gateway to Greater China & Global Markets" in `72px` (or `text-5xl md:text-7xl`) Bold Satoshi with tracking `-0.02em`.
   - Implement two CTAs:
     - Blue primary action button (`#2563eb`) with a right-arrow icon.
     - White outline button with a play-circle icon ("Watch Overview").
2. **Dashboard Visual Window**:
   - Embed a simulated browser panel (`max-width: 1152px`) with a top bar containing three dots and a URL input bar (`portal.jindalsons.com`).
   - Draw a `256px` left sidebar (Slate 50 background) listing corporate compliance, filings, and audit status.
   - Draw a 3-column stats widgets row (e.g. Entities Active, Compliance Score, Pending Actions).
   - Implement the **Audit Log Table**:
     - *Columns*: Compliance Event, Date, Category, Status Badge.
     - *Events to include*: "Annual Return Filed", "HK FRS Profit & Loss Statement Compiled", "Registry Validation NNC1", "Profits Tax Registration Completed".
     - Status badges: Success (Green-100/800), Warning (Yellow-100/800), Failed (Red-100/800).
     - Right-align the time column with Slate 400 color.
     - Set a `0.2s` hover highlight transition where rows gain a light blue background (`#eff6ff`).

## Acceptance criteria

- [ ] Hero layout respects strict vertical grid parameters.
- [ ] Top badge shows pulsing status dot with appropriate color.
- [ ] Header font is Satoshi, using correct spacing and sizes.
- [ ] Simulated browser window matches standard mock layout and is fully responsive.
- [ ] Audit Log Table is built with status badges, right-aligned time strings, and smooth `#eff6ff` background on hover.
