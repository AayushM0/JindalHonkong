Status: resolved
Type: task
Blocked by: None - can start immediately

# Issue 01: Global Setup & Typography

> *This was generated by AI during triage.*

## Parent

- [.scratch/theme-redesign/PRD.md](file:///d:/Projects/Website-jindal2/.scratch/theme-redesign/PRD.md)

## What to build

Configure Satoshi typography, custom colors, animations, and baseline stylesheet utilities.
1. **Google Fonts Replacement**: Replace `IBM Plex Sans` with the Satoshi Fontshare API stylesheet link in `index.html`.
2. **Tailwind Config Update**:
   - Add Satoshi font family as primary `sans`.
   - Configure colors: `primary` (#2563eb), `slate-950` (#020617), `slate-900` (#0f172a), `deep-slate` (#151e2e), `muted-bg` (#f8fafc), and `border-slate` (#e2e8f0).
   - Configure status accents: `success` (#16a34a), `warning` (#d97706), `error` (#dc2626).
   - Extend `transitionTimingFunction` to include the custom cubic-bezier ease: `cubic-bezier(0.4, 0, 0.2, 1)`.
3. **CSS Utilities & Transitions**:
   - Configure default HTML/body stylesheet resets in `index.css` to use Satoshi with line-height 1.625.
   - Implement the `40px` light gray grid line pattern overlay as a background utility class (`bg-grid`).
   - Define custom fadeInUp animation classes (`@keyframes fadeInUp` with 20px Y-offset and 0.8s ease-out duration).

## Acceptance criteria

- [ ] Satoshi font renders correctly on all headers and text.
- [ ] Custom Tailwind colors are integrated and usable.
- [ ] Custom cubic-bezier is configured and working on hover effects.
- [ ] Grid line pattern class is available and renders correctly.
- [ ] fadeInUp animations trigger correctly.
