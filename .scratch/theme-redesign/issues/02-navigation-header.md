Status: resolved
Type: task
Blocked by: 01

# Issue 02: Navigation Header Redesign

> *This was generated by AI during triage.*

## Parent

- [.scratch/theme-redesign/PRD.md](file:///d:/Projects/Website-jindal2/.scratch/theme-redesign/PRD.md)

## What to build

Rebuild the main navigation component `Header.jsx` to match the Corporate Professional style.
1. **Dimensions & Positioning**: Re-layout the container as a fixed top header with exactly `64px` height (`h-16`).
2. **Branding & Logo**: Implement a left-aligned B2B brand identity with a Slate 950 (`#020617`) square icon and clean Satoshi bold text for "Jindalsons Limited".
3. **Glass-panel Backdrop**: Apply a flat surface with a glass-panel effect: `background: rgba(255, 255, 255, 0.7)` and `backdrop-filter: blur(12px)`.
4. **Nav Links**: Render center-aligned navigation links: `Platform`, `Solutions`, `Security`, `FAQ` in `text-sm font-medium text-slate-600`.
5. **CTA Button**: Implement a right-aligned "Book Demo" (or "Book Consultation") button using a flat solid Slate 900 background with white text and `8px` border radius (`rounded-lg`).

## Acceptance criteria

- [ ] Header height is exactly 64px.
- [ ] Navigation background uses rgba(255, 255, 255, 0.7) and 12px backdrop blur.
- [ ] Brand logo displays the slate square icon.
- [ ] Central links use text-slate-600 and transition smoothly on hover.
- [ ] Right CTA button uses Slate 900 background and 8px border radius.
- [ ] Mobile drawer menu is fully functional, using matching slate/glass-panel aesthetics.
